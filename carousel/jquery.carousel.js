;(function($) {

    var defaults = {
        items: 3,
        loop: false,
        auto: false,
        vertical: false,
    };

    $.fn.carousel = function(options) {
        var settings    = $.extend(true, {}, defaults, options),
            $elem       = this,
            $children   = $el.children();

        
    };

})(jQuery);